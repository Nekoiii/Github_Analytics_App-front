{
  "dependencies": {
    "react-datepicker": "^4.19.2",
    "axios": "^1.5.1",
    "react-scripts": "^3.4.4",
    "sass-loader": "^7.3.1",
    "webpack": "4.42.0"
  },
  "devDependencies": {
    "react-datepicker": "^4.19.2",
    "postcss": "^8.4.31",
    "resolve-url-loader": "^5.0.0",
    "sass": "^1.69.4"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "npm install && cd app && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
